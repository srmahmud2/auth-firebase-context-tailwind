/**
 * context API
 * 1. create context (export )
 * 2. create provider
 * 3. set context
 * 4. to access the value: useContext hook
 *
 */

/**
 * 1. mail.jsx er modde set AuthProvider
 * 2. inside provdier access chilredn props and then use
 */

/**
 * 1. break down the create user function
 * 2. auth provider will have auth and pass email and password from the register.
 *  
 * */
